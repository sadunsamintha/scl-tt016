<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd">


	<bean id="deviceErrorRepository" class="${deviceErrorRepository}" />

	<bean id="hardwareController" class="${hardwareController}">
		<property name="plcProvider" ref="plcProvider" />
		<property name="currentState" ref="hwDisconnectedState" />
		<property name="devicesErrorsRepository" ref="deviceErrorRepository" />
	</bean>

	<bean id="hwConnectedState" class="${hwConnectedState}">
		<property name="connectingState" ref="hwConnectingState" />
		<property name="disconnectingState" ref="hwDisconnectingState" />
		<property name="startingState" ref="hwStartingState" />
	</bean>

	<bean id="hwConnectingState" class="${hwConnectingState}">
		<property name="connectedState" ref="hwConnectedState" />
		<property name="disconnectingState" ref="hwDisconnectingState" />
	</bean>

	<bean id="hwDisconnectedState" class="${hwDisconnectedState}">
		<property name="connectingState" ref="hwConnectingState" />
	</bean>

	<bean id="hwDisconnectingState" class="${hwDisconnectingState}">
		<property name="disconnectedState" ref="hwDisconnectedState" />
	</bean>

	<bean id="hwStartedState" class="${hwStartedState}">
		<property name="stoppingState" ref="hwStoppingState" />
		<property name="disconnectingState" ref="hwDisconnectingState" />
	</bean>

	<bean id="hwStartingState" class="${hwStartingState}">
		<property name="startedState" ref="hwStartedState" />
		<property name="connectingState" ref="hwConnectingState" />
		<property name="disconnectingState" ref="hwDisconnectingState" />
	</bean>

	<bean id="hwStoppingState" class="${hwStoppingState}">
		<property name="connectedState" ref="hwConnectedState" />
		<property name="connectingState" ref="hwConnectingState" />
	</bean>

</beans>
